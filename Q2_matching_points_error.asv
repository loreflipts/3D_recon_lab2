distance = zeros(2,1);

for k = 1:7
    name = "branch"+k+"";
    
    for j = 1:length(projection_LAT.(name))
    matches_in_view2_using_view1.(name)
    projection_LAT.(name)
    distance = [distance; sqrt(((projection_LAT.(name)(1,j)-matches_in_view2_using_view1.(name)(1,j))^2+

end